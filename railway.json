{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd nrgcommerce && yarn start",
        "buildCommand": "cd nrgcommerce && yarn build",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 100,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}